# Codebase Map

> This file has two parts: an auto-generated section (regenerated by `.agent/scripts/gen-codemap.sh`) and a hand-written section that explains the architecture. **Don't edit above the marker** — your changes will be lost.

<!-- AUTO-GENERATED BELOW — DO NOT EDIT -->

_Generated by `gen-codemap.sh`. Re-run after structural changes._

## Directory tree

```
.
├── AGENTS.md -> .agent/AGENTS.md
├── apis
│   └── README.md
├── CLAUDE.md -> .agent/CLAUDE.md
├── Dockerfile
├── docs
│   └── deploy
│       └── infra-onboarding.md
├── eslint.config.js
├── IDEAS.md
├── IMPLEMENTED.md
├── index.html
├── Justfile
├── LICENSE
├── nginx.conf
├── package.json
├── package-lock.json
├── playwright.config.ts
├── README.agent.md
├── README.md
├── security-headers.inc
├── src
│   ├── App.tsx
│   ├── components
│   │   ├── Diagnostics.tsx
│   │   ├── display.ts
│   │   ├── editor-suggest.test.ts
│   │   ├── editor-suggest.ts
│   │   ├── Editor.tsx
│   │   ├── FirstRunHint.test.tsx
│   │   ├── FirstRunHint.tsx
│   │   ├── HelpModal.tsx
│   │   ├── MapView.test.tsx
│   │   ├── MapView.tsx
│   │   ├── OrientationHint.test.tsx
│   │   ├── OrientationHint.tsx
│   │   ├── Summary.tsx
│   │   ├── Timeline.test.tsx
│   │   ├── Timeline.tsx
│   │   ├── Toolbar.test.tsx
│   │   └── Toolbar.tsx
│   ├── dsl
│   │   ├── edit.test.ts
│   │   ├── edit.ts
│   │   ├── parse.test.ts
│   │   ├── parse.ts
│   │   ├── resolve.test.ts
│   │   ├── resolve.ts
│   │   └── types.ts
│   ├── geocode
│   │   ├── ambiguity.test.ts
│   │   ├── ambiguity.ts
│   │   ├── nominatim.test.ts
│   │   └── nominatim.ts
│   ├── lib
│   │   ├── colors.ts
│   │   ├── dates.ts
│   │   ├── dsl-reference.test.ts
│   │   ├── dsl-reference.ts
│   │   ├── duration.test.ts
│   │   ├── duration.ts
│   │   ├── example.ts
│   │   ├── export
│   │   ├── format.ts
│   │   ├── share.test.ts
│   │   └── share.ts
│   ├── main.tsx
│   ├── state
│   │   ├── store.test.ts
│   │   ├── store.ts
│   │   ├── use-geocoder.ts
│   │   ├── use-media-query.test.ts
│   │   ├── use-media-query.ts
│   │   └── use-resizable-height.ts
│   ├── styles.css
│   ├── test-setup.ts
│   └── vite-env.d.ts
├── Taskfile.yaml
├── test-results
├── tests
│   └── e2e
│       ├── mobile.spec.ts
│       └── plan.spec.ts
├── tsconfig.json
└── vite.config.ts
```

## Public API surface

### Go
### TypeScript
src/components/Diagnostics.tsx:8:export function Diagnostics({ diagnostics, onSelect }: DiagnosticsProps) {
src/components/display.ts:3:export type LocationState = "ok" | "manual" | "loading" | "notfound";
src/components/display.ts:6:export interface DisplayActivity {
src/components/display.ts:13:export interface DisplayDriveStop {
src/components/display.ts:20:export interface DisplayHop {
src/components/display.ts:32:export function activityQuery(activityName: string, hopName: string): string {
src/components/display.ts:37:export interface DisplayWaypoint {
src/components/editor-suggest.ts:4:export interface Suggestion {
src/components/editor-suggest.ts:9:export interface CompletionContext {
src/components/editor-suggest.ts:74:export function getCompletionContext(value: string, caret: number): CompletionContext | null {
src/components/Editor.tsx:78:export function Editor({ value, diagnostics, focusLine, focusRange, onChange }: EditorProps) {
src/components/FirstRunHint.tsx:14:export function FirstRunHint({ onShowHelp, onDismiss }: FirstRunHintProps) {
src/components/HelpModal.tsx:9:export function HelpModal({ open, onClose }: HelpModalProps) {
src/components/MapView.tsx:122:export function MapView({
src/components/OrientationHint.tsx:15:export function OrientationHint({ show }: OrientationHintProps) {
src/components/Summary.tsx:8:export function Summary({ trip }: SummaryProps) {
src/components/Timeline.tsx:36:export function Timeline({ hops, activeHopId, onHover, onAddHop }: TimelineProps) {
src/components/Toolbar.tsx:53:export function Toolbar({
src/dsl/edit.ts:15:export function formatCoords(lat: number, lng: number): string {
src/dsl/edit.ts:37:export function setHopCoords(text: string, headerLine: number, lat: number, lng: number): string {
src/dsl/edit.ts:73:export const NEW_HOP_NAME = "New stop";
src/dsl/edit.ts:89:export function appendHop(text: string): { text: string; nameStart: number; nameEnd: number } {
src/dsl/parse.ts:47:export function isValidIsoDate(s: string): boolean {
src/dsl/parse.ts:108:export function parse(text: string): ParseResult {
src/dsl/resolve.ts:4:export interface ResolveOptions {
src/dsl/resolve.ts:17:export function resolve(trip: ParsedTrip, options: ResolveOptions = {}): ResolvedTrip {
src/dsl/resolve.ts:89:export function dateDiagnostics(parsed: ParsedTrip, resolved: ResolvedTrip): Diagnostic[] {
src/dsl/types.ts:6:export type TransportMode = "flight" | "train" | "bus" | "ferry" | "car" | "walk";
src/dsl/types.ts:8:export const TRANSPORT_MODES: readonly TransportMode[] = [
src/dsl/types.ts:17:export interface Money {
src/dsl/types.ts:22:export interface LatLng {
src/dsl/types.ts:28:export interface DriveStop {
src/dsl/types.ts:37:export interface Activity {
src/dsl/types.ts:48:export interface Hop {
src/dsl/types.ts:76:export interface Waypoint {
src/dsl/types.ts:85:export interface ParsedTrip {
src/dsl/types.ts:95:export type Severity = "error" | "warning";
src/dsl/types.ts:97:export interface Diagnostic {
src/dsl/types.ts:104:export interface ParseResult {
src/dsl/types.ts:110:export interface ResolvedDriveStop extends DriveStop {
src/dsl/types.ts:117:export interface ResolvedHop extends Hop {
src/dsl/types.ts:126:export interface ResolvedTrip {
src/geocode/ambiguity.ts:22:export function distanceKm(a: GeocodeCandidate, b: GeocodeCandidate): number {
src/geocode/ambiguity.ts:36:export function isAmbiguous(candidates: GeocodeCandidate[]): boolean {
src/geocode/nominatim.ts:11:export type GeocodeResult = LatLng | null;
src/geocode/nominatim.ts:14:export interface GeocodeCandidate extends LatLng {
src/geocode/nominatim.ts:50:export interface GeocoderDeps {
src/geocode/nominatim.ts:58:export interface Geocoder {
src/geocode/nominatim.ts:83:export function createGeocoder(deps: GeocoderDeps = {}): Geocoder {
src/lib/colors.ts:17:export function colorForIndex(index: number): string {
src/lib/dates.ts:6:export function toUtc(iso: string): number {
src/lib/dates.ts:11:export function fromUtc(ms: number): string {
src/lib/dates.ts:19:export function addDays(iso: string, days: number): string {
src/lib/dates.ts:24:export function dayDiff(a: string, b: string): number {
src/lib/dates.ts:29:export function today(now: number = Date.now()): string {
src/lib/dates.ts:34:export function formatDate(iso: string): string {
src/lib/dsl-reference.ts:8:export interface DslSection {
src/lib/dsl-reference.ts:14:export const DSL_REFERENCE: DslSection[] = [
src/lib/duration.ts:8:export function parseDurationMinutes(raw: string): number | null {
src/lib/duration.ts:29:export function formatDuration(minutes: number): string {
src/lib/example.ts:2:export const EXAMPLE_DSL = `trip "Interrail Summer"
src/lib/export/download.ts:9:export function downloadTextFile(filename: string, content: string, type: string): void {
src/lib/export/ics.ts:152:export function planToIcs(trip: ResolvedTrip): string {
src/lib/export/ics.ts:168:export function icsFilename(title: string | undefined): string {
src/lib/format.ts:2:export function formatMoney(amount: number, currency: string): string {
src/lib/format.ts:18:export function pluralizeDays(days: number): string {
src/lib/share.ts:22:export function encodePlanHash(dsl: string): string {
src/lib/share.ts:31:export function decodePlanHash(hash: string): string | null {
src/lib/share.ts:44:export function buildShareUrl(dsl: string): string {
src/state/store.ts:21:export function loadCurrent(): string {
src/state/store.ts:27:export function saveCurrent(text: string): void {
src/state/store.ts:46:export function listPlans(): string[] {
src/state/store.ts:50:export function savePlan(name: string, text: string): void {
src/state/store.ts:58:export function loadPlan(name: string): string | null {
src/state/store.ts:63:export function planExists(name: string): boolean {
src/state/store.ts:67:export function deletePlan(name: string): void {
src/state/store.ts:74:export function isFirstRunHintDismissed(): boolean {
src/state/store.ts:79:export function dismissFirstRunHint(): void {
src/state/store.ts:84:export function isOrientationHintDismissed(): boolean {
src/state/store.ts:89:export function dismissOrientationHint(): void {
src/state/use-geocoder.ts:5:export type LocationStatus = LatLng | "loading" | "notfound";
src/state/use-geocoder.ts:7:export interface GeocoderState {
src/state/use-geocoder.ts:25:export function useGeocoder(names: string[]): GeocoderState {
src/state/use-media-query.ts:9:export function useMediaQuery(query: string): boolean {
src/state/use-media-query.ts:28:export const PHONE_QUERY = "(max-width: 640px)";
src/state/use-resizable-height.ts:12:export function useResizableHeight(storageKey: string, fallback: number) {
### Python

## Stats

```
Total tracked files: 109
```

<!-- HAND-WRITTEN BELOW — EDIT FREELY -->

## Architecture

`rejs` is a **frontend-only** React + Vite + TypeScript SPA for planning and visualizing longer
journeys. There is no backend. The user writes a journey in a small **DSL** (a block-per-hop text
format) in the left editor; the app renders it live on a Leaflet **map** and a Gantt-style
**timeline**, with budget and duration totals.

Data flows through one pure pipeline, owned by `src/App.tsx`:

1. **Parse** — `src/dsl/parse.ts` turns DSL text into a `ParsedTrip` plus an array of `Diagnostic`s.
   It is deliberately forgiving: it never throws and always returns the best-effort trip, so the
   UI updates while the user is mid-edit.
2. **Resolve** — `src/dsl/resolve.ts` turns the `ParsedTrip` into a `ResolvedTrip` where every hop
   has concrete start/end dates (chaining `stay:` durations off the previous hop) and budget/day
   totals are aggregated.
3. **Geocode** — `src/state/use-geocoder.ts` (a hook over `src/geocode/nominatim.ts`) resolves each
   hop's place name to coordinates via the public Nominatim API, cached in `localStorage` and rate
   limited to ≤1 req/s. A hop's explicit `coords:` bypasses geocoding entirely.
4. **Render** — `App` composes resolved hops + per-hop color + location into `DisplayHop[]` and feeds
   the `MapView`, `Timeline`, and `Summary` components. The current DSL buffer autosaves to
   `localStorage`; named plans are saved via `src/state/store.ts`.

The per-hop **color** (`src/lib/colors.ts`) is the visual thread: the same color is used for a hop's
map pin, its leg, and its timeline bar.

## Key files

| File | Purpose |
|------|---------|
| `src/App.tsx` | Top-level state + the parse → resolve → geocode → render pipeline. |
| `src/main.tsx` | React entry point; mounts `App`, imports global `styles.css`. |
| `src/dsl/types.ts` | Core data model: `Hop`, `ParsedTrip`, `ResolvedTrip`, `Diagnostic`. |
| `src/dsl/parse.ts` | Forgiving DSL parser → `{ trip, diagnostics }`. |
| `src/dsl/resolve.ts` | Date/duration chaining + budget/day aggregation. |
| `src/geocode/nominatim.ts` | Nominatim client with `localStorage` cache + rate limiting. |
| `src/state/use-geocoder.ts` | React hook mapping hop names → coordinates. |
| `src/state/store.ts` | Autosave buffer + named plan save/load in `localStorage`. |
| `src/components/MapView.tsx` | Leaflet map: colored pins + legs (dashed for flight/ferry). |
| `src/components/Timeline.tsx` | Gantt-style colored bars positioned by resolved dates. |
| `src/components/Editor.tsx` / `Diagnostics.tsx` | DSL textarea + clickable problem list. |
| `src/lib/colors.ts` / `dates.ts` / `format.ts` | Palette, UTC date math, money/day formatting. |

## Where to add things

- New DSL field → parse in `src/dsl/parse.ts`, add to the `Hop` type in `src/dsl/types.ts`, and
  consume it in `src/dsl/resolve.ts` and the components.
- New visualization → new component under `src/components/`, fed `DisplayHop[]` from `App`.
- New shared utility → `src/lib/`.
- New persisted data → extend `src/state/store.ts` (keep a versioned `localStorage` key).
- Unit tests live next to code as `*.test.ts` (Vitest); browser flows in `tests/e2e/` (Playwright).

## Conventions specific to this codebase

- The DSL parser **never throws** — bad input becomes a `Diagnostic`, not an exception.
- All date math is UTC-string based via `src/lib/dates.ts`; never construct `Date` from a local
  `YYYY-MM-DD` elsewhere (timezone drift).
- Geocoding is cached and throttled — respect Nominatim's ≤1 req/s usage policy; don't remove the
  rate limit or the cache.
- One color source of truth: `colorForIndex` in `src/lib/colors.ts`.
